# 依頼：VESPER BAND を「公開できる完成品」まで仕上げてください

あなた（GPT-6 Astra）への依頼です。この文は、依頼主「さとるん」の相棒の AI（Claude Code）が書いています。
Claude Code → codex exec → あなた、の順でつながっています。あなたはブラウザを使えません。画面での確認は Claude がブラウザで行い、写真・エラー・測った数を、この会話の続きとして返します。

## 何を仕上げるか

`toys/music_app/`（作業場所＝今いるフォルダ）は、さとるんの音楽アプリ（BLUE GARAGE STUDIO / VESPER PIANO / VESPER BAND）です。
今回の対象は **VESPER BAND（`public/band/`）**：ロボット VESPER-01 がワンマンバンドを 3D で演奏するのを見て・聴くツールです。
- 右手 = 37 鍵シンセ（途中で 8 つのつまみを回して音色を変える）、右足 = 13 本の足鍵盤（ベース）、左手 = スティック 1 本でハイハット・スネア・タム・クラッシュ、左足 = バスドラ。
- シンセは自作の AudioWorklet シンセ「VESPER SYNTH2」（`public/js/synthcore.js`・`public/js/worklet/`・`public/js/synth2.js`）。
- 作曲は画面から Claude Fable 5.1 に頼む仕組み（`band.js` の `compose`）。

さとるんの今の評価は「基本的にはめちゃくちゃいい」。そのうえで「最後までやり切って」と頼まれています。
ゴールは **さとるんが一言「出して」と言えば公開（push）できる状態** です。公開そのものは今回やりません（Claude がさとるんに確認します）。

**まず読むもの**：`HANDOFF1.md` の 15 章（VESPER BAND のすべて。できたこと・事故と教訓・次にやること）。ピアノ側の事情は同じファイルの 1〜14 章。コードは `public/band/*`、`public/js/synth2.js`・`synthcore.js`・`worklet/`、`public/js/audio.js`・`state.js` のバンド関係、`public/piano/scene.js`（ロボットの体をバンドと共有）。

## 完成の条件（全部やってください）

### 1. 同梱曲を作る（今は `public/band/repertoire/index.json` が空）
- **あなた自身が作曲した、オリジナル曲を 4 曲以上**。既存の曲の旋律・伴奏の型をそのまま使わない（権利）。
- 形式は画面の「書き出し」ボタン（`band.js` 138 行あたり）と同じ `{ song }` の `.band.json`。`demoSong()` と `bandSong()` が曲データの見本です。音色（keys と pedal の SYNTH2 パッチ）もあなたが決めて曲に入れる。
- 4 曲は性格をはっきり変える（例：速い 8 ビートのシンセウェーブ／ファンク／ゆっくりのバラードでフィルターを大きく開け閉め／タムとクラッシュが主役の速い曲 など。例に縛られなくてよい）。長さは 1 曲 1.5〜3 分。
- **つまみ操作を曲の見せ場として使う**（1 曲に数回以上。音楽的に意味のある所で）。この楽器の味は「片手なのでハイハットとスネアは同時に鳴らない」こと（さとるん「まさにこれがかっこいい」）。
- 体の限界 `LIMITS`（`band/prompts.js`）を守り、`analyzeBand`（`band/critic.js`）が **1 つも直す所・issues を出さない** 状態にする。
- `index.json` に 1 曲ずつ：`title` / `file` / `composer`（"GPT-6 Astra"）/ `license`（オリジナル。このアプリのために GPT-6 Astra が作曲、と分かる文）/ 必要なら短い説明。ピアノの `public/piano/repertoire/index.json` の書き方に合わせる。

### 2. ブラウザ無しで曲を確かめる道具
- `tools/` に Node で動く確認用の道具を作る（例 `tools/band-check.mjs`）：同梱曲を全部読み、形式の検査・`analyzeBand` の結果（直した数・issues）・音数・長さ・つまみの回数を出す。`critic.js` がブラウザ専用の物に頼っているなら、Node でも読めるように分ける。

### 3. 見た目の磨き（HANDOFF1 15.4 の 3）
- スティックの握り（指がスティックを本当に握っているか）、左足のキックペダルの見え方（踏み板・ビーターが見えて、踏んでいると分かるか）、つまみを回す手の形（指でつまんで回しているように見えるか）。
- さとるんの見た目の基準は高い：遠くから見ても近くで見ても「芸術」と言える質。低ポリゴンの角ばった調子・アニメ調・絵を貼っただけ・安い小物で賑やかすのは NG。光で形の弱さをごまかさない。

### 4. 通して読んで、不具合を直す
- バンドのコードを全部読んで、壊れている所・壊れやすい所を直す。特に：途中から再生・止める・曲を替える・もう一度再生したときに、つまみの値や音色が正しく戻るか（前の曲のつまみが残らないか）。テンポ。音が途切れる・鳴りっぱなしになる所。曲を何度も替えたときのメモリや CPU。作曲の途中で失敗したとき。表示の崩れ（狭い画面を含む）。
- 見つけたこと・直したことは最後の返事に全部書く。直していない物も書く。

### 5. ピアノを壊さない
- `public/js/audio.js`・`state.js`・`public/piano/scene.js` などはピアノ（VESPER PIANO・公開中）と共有です。変えるときはピアノが今までどおり動くことを保つ。

### 6. 引き継ぎ
- `HANDOFF1.md` の 15 章を今の状態に書き直す（何が変わったか・同梱曲・確かめ方・残り）。

## Claude が確かめやすい入口を作ってください
Claude はブラウザで `http://localhost:5173/band/` を開き、開発用の道具（`window.bandDemo()`、`window.bandRehearse()` → `{counts, misses}`、`vesperStage.debugPose()`）で確かめます。これに加えて：
- `?song=<同梱曲のファイル名>` で、その曲を開いた状態でページが立ち上がる。
- 見た目を撮るための入口（例 `window.bandShot({ view: "stick" | "pedal" | "knob" | "full" など, beat: 12.5 })`：止まったまま、その拍の姿勢にロボットを置いて、決まったカメラで写す）。同じことを URL（例 `?song=…&shot=pedal&beat=12.5`）でもできると、Claude がコマンドで写真を撮れて助かります。
- 最後の返事に「Claude がブラウザで見るべき所と、その手順」を書く。

## 決まり
- 書いてよいのは今のフォルダ（`music_app/`）の中だけ。`.git` は触らない（commit・stash・checkout・reset など、git で何かを変える操作はしない）。インターネットには出られない（npm install もできない）。鍵・秘密のファイルを探したり読んだりしない。
- **まだ commit されていない作りかけのファイルがたくさんあります**（`public/band/`・`synth2.js`・`synthcore.js`・`worklet/` など）。消したり、まとめて作り直したりしない。直すときは部分ごとに。
- JS を書き換えたら必ず `/Users/satorun/.local/node/bin/node --check <ファイル>` を通す（行末コメントで括弧を消して画面が真っ白になる事故が、別の作品で過去 5 回あります）。
- `synthcore.js` か `worklet/synth2-processor.js` を変えたら必ず `/Users/satorun/.local/node/bin/node tools/bundle-worklet.mjs` で作り直す（作り直さないと古い音のまま）。
- 素材（形・模様・音）は自作か手続き生成、または今このフォルダにある音源（Naked Drums など）。外から持ってきた素材は使わない。
- 文書に、さとるんの作業の手順・力量・段取りを勝手に想定して書かない（例：「まず小さく試して、さとるんに確認してもらってから本番」のような、本人が言っていない進め方）。

## 進め方
- 途中で止まって確認を求めなくてよいです。完成の条件 1〜6 を最後までやってください。
- 最後の返事は日本語で：
  1. **さとるん向けのまとめ**（中学生でも分かる言葉だけで。難しい言葉・専門用語・業界のカタカナ語は使わない。ただし情報は落とさない）。
  2. 変えたファイルと、それぞれ何をしたか。
  3. 自分で確かめたこと（`band-check` の結果の数など）。
  4. 確かめられていないこと（Claude がブラウザで見るべき所と手順）。
  5. まだ直したい所。
